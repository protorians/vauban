import {IControllableScheme, IRoutableScheme} from "../types/index.js";

export const ControllableScheme: Map<string, IControllableScheme> = new Map()
export const RoutableScheme: Map<string | RegExp, IRoutableScheme> = new Map()